{% extends 'base.html.twig' %}

{% block title %}{{ 'Profile' }}{% endblock %}

{% block body %}

    <h1>{{ 'Profile' }} {{ app.user.fullName }}</h1>
    <form action="{{ path('app_logout') }}" method="post">
        <button type="submit">{{ 'Logout' }}</button>
    </form>

{% endblock %}
